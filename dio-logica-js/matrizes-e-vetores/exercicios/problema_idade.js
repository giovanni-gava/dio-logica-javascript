//Crie um vetor que armazene as idades de cinco pessoas. Em seguida, crie uma variável para armazenar a maior idade do vetor e mostre o resultado no console.

let ages = [20, 25, 30, 35, 40];  // Vetor com as idades das pessoas

let maxAge = ages[4];  // Atribui a última posição como sendo a maior idade manualmente

console.log("A maior idade é " + maxAge);  // Exibe a maior idade no console
