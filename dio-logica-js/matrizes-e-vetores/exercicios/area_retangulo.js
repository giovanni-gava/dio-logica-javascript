// Crie um programa que calcule a área de um retângulo. Defina a largura como uma constante e a altura como uma variável. Em seguida, exiba o valor da área no console.

const rectangleHeight = 5;  // Altura do retângulo
let rectangleBase = 8;  // Base do retângulo

let rectangleArea = rectangleBase * rectangleHeight;  // Área do retângulo

console.log("A área do retângulo é " + rectangleArea);

