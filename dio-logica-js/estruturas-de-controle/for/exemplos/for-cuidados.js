
let lifePoints = 0;

for(let i = 0; i < 11 ; i++) {
    lifePoints += 1;
    console.log(`Ganhou 10 pontos de vida. Total: ${lifePoints}`);
}
// Esse for está declarando a variável i com valor 0 e devemos incrementar 1 a cada iteração e prestar atencao pois a contagem comeca em 0
console.log(lifePoints + " total")